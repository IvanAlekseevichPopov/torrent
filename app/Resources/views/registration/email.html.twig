Для завершения регистрации просьба перейти по ссылке

<a href="{{ confirmationUrl|raw }}" target="_blank">{{ confirmationUrl|raw }}</a>

Или вставить уникальный код на форме завершения регистрации:

{{ token }}
